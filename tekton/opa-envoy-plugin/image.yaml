apiVersion: tekton.dev/v1alpha1
kind: PipelineResource
metadata:
  name: opa-envoy-plugin-image-test-auth-login
spec:
  type: image
  params:
    - name: url
      value: docker.io/yerinu2019/test-auth-login #configure: replace with where the image should go: perhaps your local registry or Dockerhub with a secret and configured service account
